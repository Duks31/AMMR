### PLEASE CONFIRM SERVICES BEFORE RUNNING THIS DOCKER COMPOSE FILE ON RPI

services:
  # ── Navigation: SLAM + Nav2 ──────────────────────────────────────
  # cika_navigation:
  #   build:
  #     context: ..
  #     dockerfile: docker/Dockerfile
  #   image: cika_ros2:humble
  #   container_name: cika_navigation
  #   network_mode: host
  #   privileged: true
  #   volumes:
  #     - ../cika_ws/src:/cika_ws/src
  #     - ../cika_ws/config:/cika_ws/config
  #     - ../cika_ws/maps:/root/.ros
  #   environment:
  #     - ROS_DOMAIN_ID=31
  #     - RMW_IMPLEMENTATION=rmw_fastrtps_cpp
  #   tmpfs:
  #     - /tmp:size=200m
  #   command: ros2 launch cika_bringup navigation.launch.py
  #   restart: unless-stopped

  # ── Perception: OAK-D + YOLO ────────────────────────────────────
  # cika_perception:
  #   image: cika_ros2:humble
  #   container_name: cika_perception
  #   network_mode: host
  #   privileged: true
  #   volumes:
  #     - ../cika_ws/src:/cika_ws/src
  #     - ../cika_ws/models:/cika_ws/models
  #   devices:
  #     - /dev/bus/usb:/dev/bus/usb
  #   environment:
  #     - ROS_DOMAIN_ID=31
  #   tmpfs:
  #     - /tmp:size=200m
  #   command: ros2 launch cika_perception perception.launch.py
  #   restart: unless-stopped

  # ── Manipulation: MoveIt2 + Arm ──────────────────────────────────
  # cika_manipulation:
  #   image: cika_ros2:humble
  #   container_name: cika_manipulation
  #   network_mode: host
  #   privileged: true
  #   volumes:
  #     - ../cika_ws/src:/cika_ws/src
  #     - ../cika_ws/config:/cika_ws/config
  #   devices:
  #     - /dev/ttyUSB0:/dev/ttyUSB0
  #   environment:
  #     - ROS_DOMAIN_ID=31
  #   tmpfs:
  #     - /tmp:size=200m
  #   command: ros2 launch robot_arm_controller main_launch.py
  #   restart: unless-stopped
